let uniqueIdentifier: number | string = 1000;

uniqueIdentifier = '42asd5852-85asdf285';

//Also applies to Array

const keys: (number | string)[] = ['This', 123];

// Best Use of UNION Type

let courseId: number | null = 1000;
// let courseId: number = 1000;

courseId = null;
